# Fly v85 배포 완료 — 2026-09-05

사용자가 Ch8·9/아트 배치 종료 후 “푸시 배포 해줘”라고 명시적으로 승인했다.

- 배포 코드: `7eac52668bf8e9a46d213cf53983cec3d84b68ff`, origin/main 푸시 성공.
- 이미지: `registry.fly.io/poker-doku:git-7eac526`, digest `sha256:6cfc29a4b1b431decc785877d22955b204f62fd5b44adfec54aad5b3bbd9a9f4`.
- 명령: `flyctl deploy --remote-only --ha=false --image-label git-7eac526 --wait-timeout 300s`. Fly 원격 프로덕션 빌드와 배포 명령 exit0.
- Fly release **85**, 기존 `48ed666a50d2e8` 머신1대, 도쿄 `nrt`, `started`, health `passing`. 자동 정지는 계속 off이고 기존 암호화 `/data` 볼륨을 유지했다.
- 머신 갱신09:39:20 KST, health 통과09:39:38 KST, 외부 제공 검증09:39:59 KST.
- 배포 전 당일04:00 KST 암호화 DB 백업이 존재함을 확인했다. 기존 프로세스는 정상 종료 code0. 새 프로세스 시작 직후의 일시적인 listening/health 경고는 이후 통과 상태로 해소됐다.

## 운영 검증

`https://poker-doku.fly.dev/healthz`의 `{ok:true}`, 메인 페이지 HTTP200, 실제 WSS Engine.IO handshake를 확인했다. 게임/프로필을 생성하는 소켓 명령은 전송하지 않았다.

신규 CG8개와 영상2개의 MP4+WebM4개, 합12개를 운영 주소에서 다운로드해 승인 manifest SHA256과 모두 일치함을 확인했다. 이는 운영 파일 제공 검사이며 신규 계정으로 전체 챕터를 플레이한 테스트는 아니다. 기능/컴포넌트 검증은 `2026-09-05-r3-final-qa.md`, `2026-09-05-art-first-supply-qa.md`에 있다.

로컬 증거: `qa-tmp/expansion-reviews/fly-deploy-7eac526.log`, `fly-status-7eac526.json`, `deployment-7eac526-verification.json`. 배포 전 Fly v84 이미지 참조는 `registry.fly.io/poker-doku:deployment-01M1N5RTQ83YQF2Z5MKTXDVM0E`다. DB 마이그레이션이 포함되어 있으므로 이미지 되돌리기와 데이터 복원은 같은 작업으로 간주하지 않는다.

이 문서와 인계 상태 갱신만 후속 문서 커밋으로 푸시한다. 실행 코드/아트는 배포한 `7eac526` 그대로이며 문서 때문에 다시 배포하지 않는다. Ch10~12는 다음 세션 준비 상태를 유지한다.
