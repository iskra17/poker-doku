# 다음 세션 준비 — Ch10~12

## 사용자 결정

2026-09-05 토큰 비용을 줄이기 위해 이번 세션은 **Ch8·9와 일반 아트 연결까지만 마무리**한다. 이 문서는 다음 작업을 준비하는 인계이며 R4 구현을 시작하라는 현재 세션 지시가 아니다. push/deploy는 별도 사용자 지시가 필요하다.

## 재사용할 결정과 근거

- **완료 기준 코드 `09fd17e`**: Ch1~9 연결 완료, v36, CG8장/영상2개 공급. R3b `a931638`, A3 `7d1bf1a`/`3dde4ff`를 포함한다. 전체 검사 후7개 고정 기대값/문구 문제를 보정해 관련58검사를 재통과했고 최종 고유2,623통과/2skip, build/lint/tsc 완료. 새 전체 감사를 시작하지 않는다.
- 최종 계약: `../specs/2026-09-05-act3-act4-design.md`, 특히 마지막 ‘Fable 검토 후 총괄 확정 계약’. 실제 Fable 5.1 xhigh 기획 검토는 이미 완료했다. 같은 기획을 다시 전체 리뷰하지 않는다.
- 코드/검증 현황: `2026-09-05-expansion-execution.md`의 최종 통합 기록과 main 커밋을 먼저 확인한다. 완료된 R1/R2/A2/R3를 처음부터 감사하지 않는다.
- 역할은 Astra X-High 총괄, Astra Medium 또는 Opus 구현이다. 하위 작업자 최대2명(CLI 포함), GPU 소유자1명. 범위가 명확한 구현은 관련 테스트로 확인하고, 새 심층 리뷰는 실제 미해결 경계가 있을 때 좁게 한정한다. 이 세션에서 완료 Fable 리뷰5회의 출력만 추론 포함265,581토큰이 사용되어 사용자 비용 우려가 생겼다.

## 제안 실행 순서

1. **R4a 드릴**: MDF와 SnG 문항을 기존 생성기/해설/공개 DTO/복습 계약에 추가한다. MDF=P/(P+B), 콜 필요 승률=B/(P+2B)를 구분하고, 명시된 스택·포지션·가격·상대 가정에서만 정답을 만든다.
2. **R4b Ch10·11**: 이미 확정된 드릴·프리셋·스파링·실패 재도전·보상 연결. Ch10 두 번째 HU 실패 시 HU만 재도전. Ch11은30핸드, any-k(3)와 실제 오픈/c벳/밸류 기회 중1회 실행 확인을 함께 적용한다. 기회0을 성공으로 세거나, 전부 프리플랍 폴드해서 실천 조건을 지우지 않는다.
3. **R4c Ch12 졸업**: 실제6인 practice SnG,1,000칩/2분 구조, 일반 SnG1,500칩/3분은 유지한다. 실제 순위로 종료하고 부재 중에도 블라인드/자동액션을 계속한다. 일반 수련 cash hold/파산 리필/중복 일반SnG XP 경로를 적용하지 않는다.
4. **졸업 영속/재도전**: 실제 순위 receipt와 ITM/우승 자격은 에필로그 전 같은 트랜잭션으로 기록한다. full 완료와 첫 XP/인연도 함께 확정한다. 졸업 대결만 재도전은 durable Ch12 완료로 자격을 확인하며 새1,000칩·드릴null·XP/인연/칩0. 검은띠는 전체4막 완료와 ITM 자격의 교집합이다. Ch12 exam은 UI/서버 모두 거절한다.

R4 마이그레이션은 현재 최신 버전을 확인해 다음 번호를 사용한다. 적용된 v34/v35/v36은 수정하지 않는다. 없는 CG/의상은 AVAILABLE에 등록하거나 기존 이미지로 새 보상인 척하지 않는다.

## 검증 범위

각 구현자는 관련 테스트만 `--maxWorkers=2`로 실행한다. 총괄은 의미 있는 통합 배치당 전체 Vitest `--maxWorkers=4`1회와 build/lint/tsc를 실행한다. 이미 통과한 동일 검사를 재실행하려면 변경·실패·미해결 위험의 근거가 있어야 한다. 브라우저는 Ch10 실패HU 재도전, Ch11 실행확인, Ch12 순위 결산/재접속/졸업만 재도전의 핵심 흐름에 집중한다. 운영자 스킵·합성 DTO·실제 게임 플레이를 기록에서 구분한다.

## 다음 세션 시작용 문장

> 이 인계와 R3/R4 확정 설계를 읽고, 현재 main의 Ch1~9 완료 상태를 확인한 뒤 Ch10~12를 구현해줘. 완료된 기획·리뷰·테스트를 처음부터 반복하지 말고 토큰 사용을 줄여줘. 실제 졸업 SnG와 영속 보상 경계를 우선 검증하고, 배포는 하지 마.

main은 위 검증 코드와 이 인계를 함께 ff 반영한다. push/deploy는 하지 않았으며 운영은 기존 Fly v84 그대로다. QA 서버/전용 브라우저는 종료했고 ComfyUI는 준비 상태(실행0/대기0)로 남겼다. 새 세션은 현재 main 해시를 읽은 뒤 이 지점에서 이어간다.

남은 플레이 관찰은 랜덤 리딩/체크레이즈 기회 빈도와 체감 길이이며 평균 발생률은 아직 측정하지 않았다. Ch8 S 전용 신규 아트도 후속 공급 과제다. `2026-09-05-r3-final-qa.md`와 `2026-09-05-art-first-supply-qa.md`에 실제 소켓, 합성 DTO 화면, 영상 검증의 범위를 구분했다.
