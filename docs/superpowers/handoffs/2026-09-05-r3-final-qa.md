# Ch8·9 통합 QA — 2026-09-05

구현 원본 `4ccb290`, control 통합 `a931638`, 최종 보정/검증 코드 `09fd17e`. 사용자 지시에 따라 Ch8·9와 아트 연결을 마무리하고 Ch10~12는 다음 세션으로 남긴다.

## 기능과 서버 검증

- Ch8·9 전체 콘텐츠, 7/8드릴, 각2프리셋, 12/15핸드 스파링, 실패 씬과 재도전, v36 보상과 3막 갈색 펠트 연결.
- 실제 Socket.io10검사: full/exam 전체 흐름, 합법 포커 액션으로 두 리버 질문 발급·채점, 답 ACK 비공개·중복·타프로필·만료·소켓 교체·끊김·방 소멸. 구현자 상세는 `../plans/2026-09-05-r3b-live-plan.md`.
- 실패 재도전은 스파링 시작 경계의0칩 주입으로 확인했다. 임의 상태 주입 없이 보스를 이긴 플레이 기록으로 해석하지 않는다. 일반 full 검사는 기회0 미측정 계약을, 실제 행동의 정오 판정은 별도 공개 리플레이 회귀를 확인했다.

## 브라우저

- 실제 `ReadingPanel`을 합성 공개 DTO로 렌더링했다. 320×740/390×844/1280×900에서 내 카드·보드·긴 레인지·콜 가격·두 답변·긴 해설·마지막 계속 버튼이 내부 스크롤로 도달 가능하며 가로 넘침이 없다. Tab 초점이 모달 안에 남고 답변/계속 콜백이 전달된다.
- 실제 `PokerTable`과 `FeltPreview`에 임시 장착 스냅샷을 넣어 갈색 표시를 확인했다. 처음에는 DOM의 CSS 변수 참조만 있고 실제 선언이 없어 배경이 투명했다. 런타임 색3개를 `:root`로 이동한 뒤 computed gradient가 RGB(85,56,35)→(40,26,16)이며 화면도 갈색임을 확인했다. 일반 테이블과 파이널 테이블은 갈색 펠트를 적용하지 않는다.
- 이 검증은 실제 컴포넌트의 표시 검사이며 사용자가 Ch8·9를 처음부터 끝까지 직접 플레이한 기록은 아니다. 임시 라우트는 삭제했다.
- 증거: `qa-tmp/expansion-live/r3-reading-ui-results.json`, `r3-reading-*-320/390/1280.png`, `r3-brown-felt-results.json`, `r3-brown-felt.png`.
- 아트8CG/영상2개의 실제 재생·대체 포맷·정지 이미지 대체는 `2026-09-05-art-first-supply-qa.md`에 별도 기록했다.

## 통합 회귀

- 전체 Vitest 216파일: 2,616통과/7실패/2skip. 실패는 v35→36 마이그레이션 기대값3건, 갤러리 수2건, 수기 드릴 목록1건, 책장 접기 문구가 포커 용어 검사에 걸린1건이다.
- 마이그레이션 기대값은 최신 목록에서 유도하고 assertion 전에 DB 정리 핸들을 등록했다. 새 칭호/CG/드릴 수를 갱신하고 사쿠라의 책 대사는 ‘표시를 남겼다’로 다듬었다. 수정 후 실패 파일4개 전체58검사 통과. 같은 검사를 중복 합산하지 않은 최종 고유 검사는 2,623통과/2skip이다.
- 전체 lint 오류0/기존 engine 경고1, 수정4파일 ESLint 통과. CSS 표시 수정은 브라우저에서 검증했다.
- 전체 검사를 다시 반복하지 않았다. `npm run build`와 `npx tsc --noEmit` 통과. 첫 빌드는 삭제한 QA 라우트를 가리키는 `.next/dev/types` 자동 생성 파일 때문에 중단됐고, 해당 파일2개만 제거한 뒤 성공했다. 최종 라우트에 QA 화면은 없으며 프로덕션 CSS에도 갈색 색상 선언이 존재한다. `git diff --check` 통과.

## 남긴 점

랜덤 대결에서 리딩/체크레이즈 기회의 빈도와 체감 길이는 추가 플레이로 조정할 수 있다. 이번 검증은 평균 발생률을 측정하지 않았다. Ch8 S 전용 신규 아트는 없으므로 기존 의상/CG를 새 보상으로 중복 등록하지 않았다. 기능 완료 범위를 넘어선 제작과 Ch10~12 구현은 다음 세션에서 진행한다.
