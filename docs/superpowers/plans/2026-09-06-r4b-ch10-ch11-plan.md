# R4b Ch10·Ch11 구현 계획 — Fable 총괄 확정

> 2026-09-06. 총괄 Claude Fable 5.1(max). 구현 Claude Opus(`claude-opus-5`) 단일 작업자, 워크트리 `.worktrees/story-r4b-act4`
> (브랜치 `feat/story-r4b-act4`, 기준: R4a 병합 뒤 main). 근거: [3·4막 확정 설계](../specs/2026-09-05-act3-act4-design.md) 「R4: Ch10~11」과
> 「Fable 검토 후 총괄 확정 계약 R4 1항」. R4a가 등록한 `mdf-*`·`sng-*` 템플릿과 `sng-thresholds.ts`를 그대로 쓴다.

**목표:** Ch10(비비안)·Ch11(파트너)을 레지스트리에 연결하고, 필요한 공유 계약(목표 kind 5종·체크리스트·동적 복습 슬롯·히로인 채움 라인업)을
한 담당자가 먼저 확정한다.
**제외:** Ch12·졸업·토너먼트 정책(R4c), 새 아트 등록, 완료 트랜잭션 변경(R4c), MTT, 하드 모드. 기존 Ch1~9 목표 판정 완화 금지.

## 0. 순서와 중간 커밋

1. 공유 계약(1~3항) + 단위 테스트 → 커밋 `feat(story): act 4 shared contracts (objectives, checklist, review slots)`.
2. Ch10·Ch11 데이터(4~5항) + 수기 문항 2 + 카탈로그 v37(6항) + UI 소폭(7항) + 재도전 회귀(8항) → 커밋 `feat(story): add chapters 10-11 (storm call, all-round drill)`.

## 1. 목표 kind 5종 — `types.ts OBJECTIVE_KINDS`, `objectives.ts`, `views.ts`

`HeroHandFacts`에 추가(전부 공개 replay + 히어로 카드에서만 파생, 상대 홀카드 미사용):

| fact | 정의 |
|---|---|
| `calldownOpportunity` / `calldown` | 리버에 벳을 맞음(toCall>0), 그 시점 생존 상대 1명(헤즈업), 스택>콜(올인 아님), 벳 ≤ 벳 전 팟 100%(`betToPot ≤ 1.0`), 히어로 `isTopPairOrBetter`, **같은 핸드 플랍 또는 턴에서 벳을 콜한 적 있음**. 실행 = 콜; 홀카드 관여 투페어+(`strongMade`)면 레이즈/올인도 실행 |
| `airReraise` (위반 수) | 포스트플랍 어느 스트리트든 벳을 맞고(toCall>0) 레이즈 또는 벳을 넘기는 올인을 했는데 히어로가 톱페어 미만·스트레이트+ 미만이고 `countHeroOuts` < 8(리버 0) |
| `overbetOpportunity` / `overbetCorrect` | 리버, 헤즈업, 스택>콜, 벳 > 벳 전 팟 100% → 기회. 정답 = 홀카드 관여 투페어+(`rank ≥ two-pair && evaluateHand(hole,board).value > evaluateHand([],board).value`)면 콜/레이즈, 아니면 폴드 |

kind: `topair-calldown`(ratioView 기회/실행, `finalOpportunityCap` 허용 목록에 추가) · `no-air-reraise`(위반 ≤ maxCount) · `overbet-decision`(ratioView 기회/정답) ·
`executed-any`(기회 = openRaiseOpportunity+cbetOpportunity+riverValueBetOpportunity 합, 실행 = openRaise+cbet+riverValueBet 합, `target` 기반 → 기회 0이면 null) ·
`any-k-of`(체크리스트 부모).

**체크리스트**: sparring step `checklist?: { id: string; label: string; k: number; items: Objective[] }`. `evaluateObjectives(objectives, tally, extras, checklist?)`가
부모 view(kind 'any-k-of', primary true, progress = 달성 항목 수, target = required = `min(k, 판정 가능 항목 수)`, achieved = 판정 가능 0이면 null, 아니면 달성 ≥ required)와
항목 view(`primary: false, group: 'checklist'`)를 함께 돌려준다. `ObjectiveProgressView.group?: 'checklist'`(views.ts). `liveScore`·`primaryObjectivesMet`·
`primaryObjectivesAllAchieved`는 group 항목을 제외한다(부모만 본다). 검증기: 항목 id도 챕터 전역 유일, k ≥ 1, 항목 kind 유효.

경계 테스트(objectives.test): 보드만 투페어(홀카드 미관여) 오버벳 → 폴드가 정답 · 8아우츠 드로우 리레이즈 → 위반 아님 · 벳 = 100% 팟은 콜다운 기회(오버벳 아님), 101%는
오버벳 · 멀티웨이 리버 벳 → 두 kind 모두 기회 아님 · 플랍·턴 콜 없이 리버만 맞으면 콜다운 기회 아님 · 콜다운 상대 올인(스택 ≤ 콜) 제외 · 체크리스트 판정 가능 5→required 3,
2→2, 0→null · executed-any 오픈 기회 3회 전부 폴드 → false, 세 기회 모두 0 → null · 기존 Ch1~9 픽스처 판정 불변.

## 2. 동적 복습 슬롯 — Ch11 종합 8문

- `DrillSlot.templateId`에 sentinel `'*review'` 허용. drill-set step에 `reviewPool: readonly string[]`(등록된 **생성** 템플릿 id). 검증기: sentinel이 있으면 reviewPool이
  비어 있지 않고 전부 registry 존재, sentinel 없는 스텝은 reviewPool 금지.
- 코디네이터 `enterDrillSet`: sentinel 슬롯을 순서대로 ① `repository.listAll(profileId)` 복습 노트의 templateId(dueAt 오름차순, 중복 제거, registry 존재) ② 부족분은
  reviewPool에서 `hashSeed(run.runId, step.id, 'pool', i)`로 선택(세트 안 중복 최소화 — 풀보다 슬롯이 적으면 중복 없음). seed는 항상 `hashSeed(run.runId, step.id, slotIndex)` —
  **노트의 seed 재사용 금지**(같은 문제 암기 방지). exam 모드도 동일. `drill_attempts.category`는 해석된 템플릿 category.
- `dailyPool`·`chapterSkillCategories`는 sentinel을 건너뛰고 reviewPool의 id를 대신 센다(허브 스킬 칩은 기존 상한 규칙대로 표시).
- 테스트(coordinator): 노트 3개 + 풀 → 앞 3슬롯 노트 유형·나머지 풀 · 노트 0개 → 전부 풀 · 같은 runId 재생성 결정론 · 노트 seed와 다른 seed · 공개 DTO의 templateId는
  해석된 실제 id(sentinel 미노출) · exam 모드 sentinel 해석.

## 3. 라인업 토큰 `'heroine-fill'`

adapter `resolveLineupCharacter`: 'partner'를 먼저 해석(라인업 순서상 partner 좌석을 앞에 둔다), 'heroine-fill'은 `STORY_HEROINE_IDS` 중 아직 사용되지 않은 첫 id.
검증기: sparring 라인업에서만 허용, 히로인 좌석 총수(partner + fill + 명시 히로인) ≤ 6, BOT_CHARACTERS 검사 예외. 테스트(adapter): 파트너 sakura →
sakura + 로스터 순 첫 4명(중복 없음) · 파트너 없음 → 첫 미사용 히로인 규칙으로 5명 전원 히로인 · 명시 히로인과 fill 충돌 없음.

## 4. Ch10 「폭풍 속의 콜」 — teacher `vivian`, act 4 order 1, belt `brown`, requires `[...STORY_CURRICULUM[3]]`, estimatedMinutes 22

steps: intro 씬(비비안 무대 은유·반말 「너」) → lesson(개념 카드: MDF vs 콜 필요 승률 숫자 구분 · 콜다운 조건(헤즈업·≤팟·톱페어+·이미 콜한 배럴) · 에어 리레이즈 금지(강한
드로우 세미블러프는 예외) · **이 챕터의 오버벳 가정: 폴라라이즈 — 원페어 폴드, 홀카드 관여 투페어+ 콜** · 기회가 없으면 미측정) → drill-set 8(per-run):
`breakeven-fold-pct`, `breakeven-choice`, `mdf-defend-pct`, `mdf-vs-call-equity`, `call-decision`, `call-river-range`, `act-ch10-triple-barrel-call`, `act-ch10-overbet-fold` →
practice '트리플 배럴 콜다운'(잉그리드; hero `Kh Qs`, villain `Jh Td`, board `Kc 9d 7s 4h 2c`) → practice '오버벳 폴드'(드라코; hero `Ah 9s`, villain `Kd Kh`, board `Kc 9d 4s 2h 7c`) →
sparring 1(잉그리드·드라코, 100BB, maxHands 12, minHands 6; primary `ch10-calldown` topair-calldown target 2 finalOpportunityCap, `ch10-air` no-air-reraise maxCount 0;
bonus survive) → analysis 씬 → sparring 2 HU(비비안 `role:'boss'`, 100BB, maxHands 20, minHands 없음; primary `ch10-overbet` overbet-decision minRatio 0.5, `ch10-air-hu`
no-air-reraise maxCount 0; bonus `ch10-hu-chips` net-chips target 0 params bb) → epilogue 씬 → result. `failScene`(비비안). rewards: first dojo 300,000 · vivian 100,000 ·
badgeId `story-title-storm-caller`; replay 50,000; gradeBonus A 50,000 / S 120,000. 씬 문체는 기존 Ch7 비비안 대사(「자기」·무대 은유)를 따르되 반말/존댓말은 Ch7과 동일하게.

수기 2문(`authored/act4.ts`에 추가, category `action-judgment`, 비비안 말투, facts는 `readRangeFacts`/`estimateEquity` 실측 — 값 추정 금지):
- `act-ch10-triple-barrel-call`: 리버, 히어로 K♥Q♠(톱페어), 보드 K♣9♦7♠4♥2♣, 잉그리드 플랍 ½·턴 ⅔·리버 ¾팟 3배럴 — 팟 900·콜 300(필요 승률 25%). note 가정 "잉그리드
  리버 벳 레인지: 밸류 `KJ+, 99, 77, 44`(정확한 표기는 parseRange 검증) + 미스 드로우 블러프 `QJ, JT, T8`" 명시 → 정답 `call`.
- `act-ch10-overbet-fold`: 리버, 히어로 A♥9♠(원페어), 보드 K♣9♦4♠2♥7♣, 드라코 리버 2×팟 오버벳 — 팟 600·벳 1,200(콜 1,200, 필요 승률 40%). note 가정 "드라코 오버벳 =
  투페어+ 밸류 또는 미스 드로우 블러프(`QsJs, JsTs, Ts8s`…)" → 정답 `fold`. 해설은 폴라 가정 아래 원페어의 승률이 40% 미만임을 콤보 수로 보인다.

## 5. Ch11 「종합 수련」 — teacher `partner`, order 2, belt `brown`, requires `[...STORY_CURRICULUM[3]]`, estimatedMinutes 28

steps: intro 씬(speaker 'partner') → lesson(체크리스트 5항목·3개 통과·실행 확인·미측정 규칙·30핸드 고정) → drill-set 8 × `'*review'`(reviewPool = 1~3막 생성 템플릿 전부:
`rank-*`, `pos-*`, `range-open-decision`, `range-percentile`, `outs-count`, `odds-*`, `equity-estimate`, `call-decision`, `call-river-range`, `breakeven-*`, `size-*`,
`type-*`, `range-3bet-decision`, `range-vs-3bet`, `combo-*`, `read-*`, `nuts-*`) → sparring(30핸드 고정: maxHands 30, minHands 없음; lineup `['partner','heroine-fill'×4]`
100BB; primary `[ch11-execute: executed-any target 1, label '실제 오픈·c벳·밸류 기회 중 1회 이상 실행']`; checklist `{ id:'ch11-checklist', label:'체크리스트 3/5', k:3,
items: ch11-no-junk no-junk-entry maxCount 0 · ch11-no-limp no-limp maxCount 0 · ch11-cbet cbet-when-aggressor minRatio 0.67 · ch11-value value-bet-river minRatio 0.67 ·
ch11-odds correct-pot-odds-call maxCount 2 }`; bonus survive) → 하나 리뷰 씬(speaker 'hana', 분석 존댓말) → epilogue 씬(partner) → result. `failScene`(partner).
rewards: first dojo 350,000 · affinity `[{partner,100_000},{hana,20_000}]` · badgeId `story-title-all-rounder`; replay 50,000; gradeBonus A 50,000 / S 120,000.
파트너가 하나면 `firstClearRewards`가 120,000으로 합산됨을 테스트로 고정. 파트너 없음(미야코 대체)이면 partner 몫 미지급(기존 규약).

## 6. 카탈로그 v37 — `catalog.ts` + `migrations.ts` v37 `story_rewards_act4a`

Ch10: `story-title-storm-caller`(첫 완주) · `story-chips-act4-ch10-first` 500 · `story-chips-act4-ch10-s` 300. Ch11: `story-title-all-rounder` · `story-chips-act4-ch11-first` 500 ·
`story-chips-act4-ch11-s` 300. 없는 CG/의상 등록 금지. `database.test.ts` 버전 상수 36→37과 패리티 갱신. 칭호는 `lib/cosmetics/titles.ts STYLE` 한 줄 추가.

## 7. UI 소폭

- ObjectiveHud·결산 목표 리스트: `group === 'checklist'` 항목은 부모 아래 들여쓰기·'체크리스트' 표기, 부모는 `n/k 달성`(`story-live-rules.ts ObjectiveHudLine.group`,
  `formatObjectiveDetailProgress` 유지). 미션 전체보기 모달도 같은 라인 사용.
- 허브 스킬 칩: Ch11은 reviewPool 카테고리로 표시(기존 칩 상한 규칙 유지).

## 8. Ch10 HU만 재도전 회귀

기존 checkpoint 계약으로 동작해야 한다(코드 변경 없이 테스트로 고정): 스파링 1 통과 후 HU 실패 → `sparringRetry` → 새 런이 HU 스텝 index로 진입, `liveResults`에
스파링 1 요약 보존, 드릴 세트·연습 반복 없음; 재도전에서 HU 통과 → 결산 통과·두 스파링 요약 합산; 스파링 1 실패 시 스파링 1부터. 코디네이터 테스트에 Ch10 픽스처(fake adapter)로 추가.

## 9. 검증·완료 조건

```text
npx vitest run src/lib/story/objectives.test.ts src/lib/story/chapters/chapters.test.ts src/lib/story/chapters/act4/act4.test.ts \
  src/server/story-run-coordinator.test.ts src/server/story-live-adapter.test.ts src/lib/story/story-hub-rules.test.ts src/lib/story/story-live-rules.test.ts \
  src/lib/story/drills/generator.test.ts src/lib/story/drills/r4-templates.test.ts src/server/persistence/database.test.ts \
  src/server/story-reward-service.test.ts src/server/story-reward-repository.test.ts src/lib/story/grading.test.ts --maxWorkers=2
npx tsc --noEmit · npx eslint <변경 파일> · git diff --check
```

완료 = 위 통과 + 두 커밋 + 보고(모델 ID·변경 파일·테스트 수·미확정 가정). 전체 suite/build/서버/브라우저/배포는 총괄. 계획·검토 문서를 커밋에 포함.

## Astra 검토 반영 (2026-09-06, 총괄 확정 — 위 1~8항보다 우선)

검토 원문 [reviews/2026-09-06-r4b-act4-astra.md](../reviews/2026-09-06-r4b-act4-astra.md)(`gpt-6-astra`, high). 지적 1~5 수용, 6은 테스트 주의만.

1. **Ch10 facts 정밀화**: 헤즈업 = 그 시점까지 폴드하지 않은 상대 수 1(올인 상대 포함). 히어로 스택 = 시작 스택 − 누적 기여, 벳 비율은 상대 액션 직전 팟 P와 벳 B로
   계산(`raise/all-in.amount`는 스트리트 총액이므로 B = amount − 그 상대의 이전 스트리트 벳). "스택>콜"은 히어로 조건이며 상대 올인 벳도 기회다. 리버에서 처음 맞은 벳만
   기회로 세고(재레이즈 재대면은 집계 제외), 올인 레이즈 판정은 기존 `aggressive`(amount > maxBet) 공용. `riverAirBet`(자유 벳)과 `airReraise`(벳 대면)는 별개 사실.
   **홀카드 관여 메이드 판정** — 새 순수 함수 `heroMadeWithHole(hole, board)`: 히어로 카테고리 > 보드만 카테고리이면 true; 같은 카테고리면 페어 계열(원페어~풀하우스·포카드)은
   홀카드 랭크가 페어/트립스/쿼드를 이루는 랭크에 포함될 때만, 스트레이트/플러시 계열은 최고 5장에 홀카드가 들어갈 때만 true. **키커 개선만으로는 false**(예: 보드 KK772에
   AQ). `isTopPairOrBetter`는 보드만 투페어를 true로 주므로 콜다운 기회도 `heroMadeWithHole`(투페어+) 또는 홀카드 관여 톱페어로 판정한다. 기존 Ch1~9 사실·판정은 불변.
   **리뷰 정합성**: `reviewHand(record, heroId, policy?)`에 선택 정책 `{ overbetPolarized: true }`를 추가해 Ch10 스파링(`table.reviewPolicy: 'act4-overbet-v1'`)에서는 리버
   헤즈업 오버벳(>100% 팟) 대면의 폴드(홀카드 관여 투페어 미만)를 good, 콜을 warn으로 표시한다 — 목표와 HUD 리뷰가 같은 규칙. 정책 없는 기존 챕터는 불변.
2. **체크리스트 배선**: 어댑터 `objectiveViews`가 `step.checklist`를 전달. HUD `toHudLine`이 `group`을 복사하고 정렬은 부모 바로 아래 자식 유지(primary 재정렬 시 group
   블록을 한 단위로). 부모 진행 표기는 `A/R 달성`으로 클램프하지 않는다(kind 'any-k-of'는 정수 클램프 예외). `summarizeForced`는 자식을 전부 성공으로 바꾼 **뒤 부모를
   재계산**. 검증기: 부모 id도 전역 유일, k는 양의 정수, 자식 kind는 비율/상한형만(any-k-of·executed-any 중첩 금지).
3. **executed-any 전용 facts**: 기존 사실을 바꾸지 않고 `execOpenOpportunity/execOpen`(오픈 기회 동일, 실행은 `amount > maxBet`인 레이즈/올인만 — 실질 콜 올인 제외),
   `execCbetOpportunity/execCbet`(cbet 동일), `execValueOpportunity/execValue`(리버 첫 자유 액션 시점에 홀카드 관여 톱페어+면 기회 확정 — **이후 폴드로 지워지지 않음**,
   실행 = 그 액션이 벳/올인)를 액션 직전 prefix에서 확정한다. 테스트: 리버 체크 뒤 폴드해도 기회 유지.
4. **`*review` 계약 보완**: `StoryRepositoryPort`에 `listAll(profileId)` 추가(fake 포함). 노트 후보와 reviewPool은 **생성 템플릿만**(`getDrillTemplate(id)?.source.kind ===
   'generated'`, 수기는 seed와 무관하게 같은 문제). seed는 `hashSeed(runId, setId, slotIndex)`가 노트 seed와 같으면 seed+1로 결정론적 재선택. Ch11 답은 `chapter` 컨텍스트
   그대로 — 원본 노트는 exact (templateId, seed) 불일치로 승급·삭제되지 않고, 새 오답은 기존 규칙대로 새 노트를 만든다(테스트로 고정). `buildDailySlots`의 노트 seed
   사용은 유지.
5. **heroine-fill 예약**: fill 후보에서 `used`뿐 아니라 라인업 전체의 명시 히로인 id도 제외한다(뒤 좌석의 명시 히로인 보호). 검증기의 중복 캐릭터 검사는 반복 `'heroine-fill'`
   토큰을 예외로 한다. 파트너 null이면 첫 미사용 히로인(미야코 아님). masquerade 검증은 기존 그대로.
6. **HU만 재도전 테스트**: 실패 씬(`failure-scene`)을 `advance next`로 끝낸 뒤 `retrySparring`을 호출해야 한다(그 전엔 활성 런 때문에 story-busy). 구현 변경 없음.
